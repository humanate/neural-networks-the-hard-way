# 9 Transformer编解码器的应用：从BERT到大语言模型

## 9.1 架构分野：三种Transformer变体

Transformer原始论文提出的编码器-解码器架构，本质上是一种**序列到序列**（Sequence-to-Sequence）的建模方式：编码器负责“理解”输入序列，解码器负责“生成”目标序列。但在实际应用中，研究者们发现，许多任务并不需要同时具备编码和解码能力：

- **理解类任务**（如文本分类、情感分析、命名实体识别）：只需要编码器对输入进行充分理解，不需要生成新序列；
- **生成类任务**（如文本续写、对话、代码生成）：只需要解码器从左到右生成内容，不需要编码器对输入进行独立的双向编码。

这催生了三种架构变体：

| 架构类型 | 代表模型 | 注意力模式 | 核心能力 | 适用任务 |
| :--- | :--- | :--- | :--- | :--- |
| **Encoder-Only** | BERT、RoBERTa | 双向（全注意力） | 理解 | 分类、NER、抽取式问答 |
| **Decoder-Only** | GPT系列、LLaMA | 单向（因果掩码） | 生成 | 文本生成、对话、代码生成 |
| **Encoder-Decoder** | T5、BART | 编码器双向 + 解码器单向 | 理解+生成 | 翻译、摘要、文本到文本转换 |

其中，**Encoder-Only**（以BERT为代表）和**Decoder-Only**（以GPT为代表）是当前最具影响力的两条路线。前者将Transformer的“理解能力”发挥到极致，后者则将“生成能力”推向了前所未有的高度。

---

## 9.2 BERT：Encoder-Only的双向理解范式

### 9.2.1 BERT的核心设计理念

BERT（Bidirectional Encoder Representations from Transformers）由Google于2018年提出，其核心设计理念是：**利用Transformer编码器的双向注意力机制，预训练一个能够深度理解语言上下文的通用语言模型**。

在BERT出现之前，语言模型（如GPT-1）采用的是**单向**建模方式——从左到右逐个预测下一个词。这种方式的局限在于，模型在理解某个词的含义时，只能看到它左侧的上下文，无法同时利用右侧的信息。而自然语言中，一个词的含义往往同时依赖于左右两侧的语境。例如，“苹果”在“我吃了一个苹果”和“苹果发布了新手机”中含义完全不同，只有同时看到左右两侧的词，才能准确判断其语义。

BERT通过**掩码语言模型**（Masked Language Model, MLM）实现了真正的双向训练：随机遮盖输入序列中约15%的词元，让模型基于**左右两侧的完整上下文**来预测被遮盖的词。这种方式迫使编码器学会同时利用双向信息进行理解，从而学到的词向量表征远比单向模型丰富和准确。

### 9.2.2 BERT的输入表示

BERT的输入由三部分嵌入相加而成：

- **词嵌入**：将每个词元映射为固定维度的向量；
- **位置嵌入**：为每个位置提供位置信息，使模型能够区分词的先后顺序；
- **段嵌入**：用于区分两个句子（如句子A和句子B），支持句子对输入。

输入序列的开头添加特殊的 `[CLS]` 标记，其对应的最终隐藏状态被用作整个序列的聚合表示，用于分类任务；句末使用 `[SEP]` 标记分隔不同句子。

### 9.2.3 BERT的预训练任务

BERT的预训练包含两个核心任务：

**① 掩码语言模型（MLM）**

随机遮盖输入序列中15%的词元，让模型预测被遮盖的词。具体遮盖策略为：在选中的词元中，80%替换为 `[MASK]`，10%替换为随机词，10%保持不变。这种设计是为了缓解训练与推理之间的分布不匹配问题——如果训练时总是用 `[MASK]`，而推理时输入中根本没有 `[MASK]`，模型会难以适应。

**② 下一句预测（NSP）**

输入两个句子A和B，让模型判断B是否是A的真实下一句。这个任务旨在让模型学习句子级别的语义关系。不过后续研究（如RoBERTa）发现NSP任务对下游性能贡献有限，后来的模型大多将其去除。

### 9.2.4 预训练 + 微调范式

BERT确立了一种影响深远的“**预训练-微调**”范式：先在大规模无标注语料（如维基百科）上进行自监督预训练，学习通用的语言理解能力；然后在少量标注数据上进行微调，适配具体下游任务。

微调时，只需要在BERT的输出层之上添加一个简单的任务特定层：

- **文本分类**：取 `[CLS]` 标记的输出向量，接入一个全连接层进行分类；
- **命名实体识别**：对每个词元的输出向量分别接入分类层，判断其所属实体类别；
- **抽取式问答**：对每个词元的输出向量预测答案的起始和结束位置。

这种“加一个输出层即可适配多种任务”的设计，使BERT成为NLP领域真正的通用基础模型。

### 9.2.5 BERT的主要变体

BERT之后，研究者提出了多个改进版本，核心优化方向包括训练效率、参数效率和推理速度：

- **RoBERTa**（Facebook AI, 2019）：去除了NSP任务，采用**动态掩码**（每次训练时重新生成掩码模式而非固定），使用更大规模的数据和更长的训练时间，在多个基准上取得了优于BERT的性能。
- **ALBERT**（Google, 2019）：通过**跨层参数共享**和**因式分解嵌入**大幅减少参数量，参数量减少约89%，但计算成本并未降低（因为仍需进行12次前向传播），主要节省的是内存。
- **DistilBERT**（Hugging Face, 2019）：采用**知识蒸馏**技术，将BERT模型压缩40%，推理速度提升60%，同时保留了约97%的性能。

---

## 9.3 GPT与Decoder-Only：自回归生成范式

### 9.3.1 从GPT-1到GPT-3的演进

与BERT同期，OpenAI选择了另一条路线：**只使用Transformer的解码器模块**，构建**自回归语言模型**。GPT（Generative Pre-trained Transformer）的核心思想极其简洁：给定前文，预测下一个词。

GPT系列经历了三个关键阶段的演进：

**GPT-1（2018）**：首次验证了“无监督预训练 + 有监督微调”在生成任务上的有效性。模型规模约1.17亿参数，使用BooksCorpus数据集进行预训练，在多项NLP任务上展示了预训练的巨大潜力。

**GPT-2（2019）**：将参数规模提升至15亿，训练数据扩展至40GB的WebText。最关键的突破是展示了**零样本学习**能力——模型无需微调，仅通过设计合适的提示词，就能在多种任务上取得不错的表现。

**GPT-3（2020）**：参数规模达到1750亿，训练数据扩展至约45TB。GPT-3的里程碑意义在于，它正式确立了**上下文学习**作为大模型的核心能力：模型无需微调，仅通过提示词中提供的少量示例（Few-shot），就能“理解”任务并完成输出。

### 9.3.2 Decoder-Only的架构优势

为什么Decoder-Only架构最终战胜了Encoder-Only，成为主流大模型的选择？核心原因可以归结为以下几点：

**① 训练效率的碾压性优势**

Decoder-Only采用因果语言模型训练，目标是在序列的每一个位置预测下一个词。一篇1024个词元的文本，可以产生1024次训练信号，每一个位置都贡献梯度。而BERT的MLM训练中，只有约15%的被遮盖词元产生有效的训练信号，其余85%的词元不直接贡献损失更新。当训练数据规模达到万亿级别时，这种效率差距会被急剧放大。

**② 训练与推理的一致性**

BERT训练时输入包含 `[MASK]` 标记，但推理时输入中根本没有 `[MASK]`，这导致了训练数据分布与推理数据分布的不匹配。Decoder-Only则不存在这个问题：它的训练目标是“给定前文预测下一个词”，推理时做的也是同样的事情，行为完全对齐。

**③ 上下文学习能力的天然优势**

上下文学习的本质是将任务描述和示例作为“前文”，让模型接着“生成”答案。这与Decoder-Only的自回归生成范式天然契合。GPT-3展示的Few-shot能力，正是Decoder-Only架构“预测下一个词”这一简单目标在大规模数据上涌现出的结果。

**④ 架构简洁性**

原始Transformer的解码器包含三个子层：掩码自注意力、交叉注意力、前馈网络。GPT精简为只保留掩码自注意力和前馈网络两个子层，去除了交叉注意力模块，架构更加简洁，也更适合大规模堆叠。

### 9.3.3 现代LLM的关键技术

在现代大语言模型中，除了基础架构的堆叠之外，还有几项关键技术值得了解：

**KV Cache（键值缓存）**：在自回归生成过程中，生成第 \( t+1 \) 个词时，前 \( t \) 个词的 Key 和 Value 矩阵已经计算过且不再变化。KV Cache 将之前计算好的 K 和 V 缓存在显存中，生成新词时只需计算新词的 Query，避免了大量重复计算，是大模型推理加速的核心工程手段。

**位置编码的演进**：原始Transformer使用绝对位置编码（正弦函数）。现代LLM广泛采用**旋转位置编码（RoPE）**，通过对 Query 和 Key 向量施加旋转操作来编码相对位置信息，具有更好的外推性和长序列建模能力。

**归一化层的调整**：原始Transformer使用Post-LN（先残差再加归一化），现代LLM普遍采用**Pre-LN**（先归一化再残差），使深层网络的训练更加稳定。部分模型还引入了**RMSNorm**替代LayerNorm，在保持性能的同时降低计算开销。

---

## 9.4 大语言模型的完整训练范式

BERT确立了“预训练 + 微调”的基本框架，而现代大语言模型的训练流程在此基础上进一步扩展，形成了**预训练 → 监督微调 → 人类偏好对齐**的三阶段范式。

### 9.4.1 第一阶段：预训练

预训练是在超大规模无标注语料（数万亿词元）上训练基础模型的过程。Decoder-Only模型的训练目标是极其简单的**下一个词预测**：给定前 \( t-1 \) 个词，预测第 \( t \) 个词。通过在海量互联网文本上反复执行这一任务，模型逐渐学会了语法、事实知识、推理模式甚至一定程度的“世界观”。

预训练是算力消耗最大的阶段，通常需要数千张GPU运行数周甚至数月。这一阶段产出的模型被称为**基座模型**，它具备了强大的语言能力，但尚未学会“以助理的方式回答问题”。

### 9.4.2 第二阶段：监督微调

基座模型虽然能生成流畅的文本，但它的行为是“续写”而非“回答”。如果你问基座模型“法国的首都是哪里？”，它可能会续写出更多类似的问题，而不是直接回答“巴黎”。

监督微调（Supervised Fine-Tuning, SFT）通过使用高质量的**人工标注问答对**对基座模型进行微调，教会模型“如何像一个助理一样回答问题”。SFT的数据格式通常为指令-回答对，例如Alpaca格式或ChatML格式。这一阶段的计算量远小于预训练，但对模型最终的行为表现至关重要。

### 9.4.3 第三阶段：人类偏好对齐

SFT之后的模型已经能够回答问题，但回答的质量、风格、安全性仍可能不符合人类期望。**基于人类反馈的强化学习（RLHF）**通过引入人类的偏好信号来进一步优化模型。

RLHF的核心流程为：

1. **训练奖励模型**：收集人类对模型多个输出的排序偏好数据，训练一个能够对模型输出打分的奖励模型；
2. **强化学习优化**：以SFT模型为初始化，使用PPO等强化学习算法，以最大化奖励模型的得分为目标优化模型输出。同时引入KL散度惩罚项，防止模型偏离原始分布太远。

近年来，**DPO（Direct Preference Optimization）**等替代方案也逐渐流行，它将偏好优化直接转化为带权重的监督学习问题，绕过了奖励模型的训练和强化学习的复杂流程，实现更加简洁高效。

---

## 9.5 对比与总结：从理解到生成

让我们用一张表格来总结BERT与GPT两大路线的核心差异：

| 对比维度 | BERT（Encoder-Only） | GPT/LLM（Decoder-Only） |
| :--- | :--- | :--- |
| **使用的Transformer模块** | 仅编码器 | 仅解码器 |
| **注意力模式** | 双向（每个词可见全序列） | 单向（每个词仅可见前文） |
| **预训练任务** | 掩码语言模型 + 下一句预测 | 因果语言模型（预测下一个词） |
| **核心能力** | 理解 | 生成 |
| **代表应用** | 分类、NER、抽取式问答 | 文本生成、对话、代码生成 |
| **微调方式** | 加输出层微调 | 指令微调 + 偏好对齐 |
| **训练效率** | 较低（仅15%词元产生梯度） | 较高（每个位置都产生梯度） |
| **上下文学习** | 较弱 | 极强（架构天然优势） |
| **当前地位** | 仍是理解类任务的首选 | 大模型时代的主流架构 |

Transformer的编码器和解码器，原本是为机器翻译任务设计的“理解-生成”组合。BERT将编码器的理解能力推向了极致，GPT则将解码器的生成能力推向了前所未有的高度。两者的分野，本质上反映了NLP领域中“理解”与“生成”两类任务在需求上的根本差异。而Decoder-Only架构之所以在近年来占据主导地位，是因为当模型规模和训练数据达到一定程度时，“预测下一个词”这个极其简单的目标，竟然能够同时覆盖理解和生成两类能力——模型在学会预测下一个词的过程中，不得不同时学会理解前文的含义。

---

**本章核心要点回顾**：

- Transformer的编码器和解码器被拆开使用，分别演化出了BERT（Encoder-Only）和GPT（Decoder-Only）两条路线；
- BERT通过MLM实现双向理解，确立了“预训练+微调”范式；
- GPT通过因果语言模型实现自回归生成，在规模化后涌现出上下文学习能力；
- 现代LLM的训练流程为“预训练 → 监督微调 → 人类偏好对齐”三阶段；
- Decoder-Only架构因训练效率、推理一致性和上下文学习能力上的综合优势，成为当前大模型的主流选择。
### 6.10 掩码多头注意力（Masked Multi-Head Attention）

#### 1. 为什么需要掩码？

在训练阶段，解码器需要**并行地**预测整个目标序列。但生成任务本质上是**自回归**的：预测第 $t$ 个词时，只能看到前 $t-1$ 个词，绝不能看到第 $t$ 个词及之后的词（否则就是“偷看答案”）。

如果不加掩码，模型在预测“员”的时候，会直接看到“员”本身，导致训练失败。

#### 2. 掩码的实现

在计算完缩放点积分数 $S_{\text{scaled}} = \frac{QK^T}{\sqrt{d_k}}$ 之后、Softmax 之前，将**未来位置的分数设为负无穷**：

$$
S_{\text{masked}}(i, j) = \begin{cases}
S_{\text{scaled}}(i, j) & \text{如果 } j \leq i \\
-\infty & \text{如果 } j > i
\end{cases}
$$

这样，经过 Softmax 后，未来位置的注意力权重为 0：

$$
\text{Softmax}(-\infty) = 0
$$

#### 3. 掩码矩阵示例（序列长度 = 3）

$$
\text{Mask} = \begin{bmatrix}
0 & -\infty & -\infty \\
0 & 0 & -\infty \\
0 & 0 & 0
\end{bmatrix}
$$

- 第 1 个位置只能看到自己。
- 第 2 个位置能看到第 1、2 个位置。
- 第 3 个位置能看到第 1、2、3 个位置。

#### 4. 与编码器自注意力的区别

| 对比维度 | 编码器自注意力 | 解码器掩码自注意力 |
| :--- | :--- | :--- |
| **可见范围** | 全部位置 | 当前位置及之前 |
| **掩码** | 无 | 上三角为 $-\infty$ |
| **目的** | 理解完整输入 | 保持自回归生成特性 |

---

### 6.11 交叉注意力（Cross-Attention）

#### 1. 核心逻辑

交叉注意力是解码器与编码器之间的“桥梁”。它的 Query 来自解码器，Key 和 Value 来自编码器的输出。

$$
\text{CrossAttention}(Q_{\text{dec}}, K_{\text{enc}}, V_{\text{enc}}) = \text{Softmax}\left(\frac{Q_{\text{dec}} K_{\text{enc}}^T}{\sqrt{d_k}}\right) V_{\text{enc}}
$$

其中：

- $Q_{\text{dec}}$：解码器当前层的输出投影得到的 Query。
- $K_{\text{enc}}$：编码器最终输出投影得到的 Key。
- $V_{\text{enc}}$：编码器最终输出投影得到的 Value。

#### 2. 直观理解

- **Query（来自解码器）**：“我现在要生成下一个词，我应该关注输入序列的哪些部分？”
- **Key / Value（来自编码器）**：“输入序列的每个位置分别提供了什么信息？”

交叉注意力让解码器在生成每个词时，都能**动态地从编码器的全部输出中提取最相关的信息**。例如，在翻译任务中，当解码器生成“apple”时，它会通过交叉注意力重点关注编码器输出的“苹果”位置。

#### 3. 与自注意力的对比

| 对比维度 | 自注意力（Self-Attention） | 交叉注意力（Cross-Attention） |
| :--- | :--- | :--- |
| **Q 来源** | 同一序列 | 解码器 |
| **K、V 来源** | 同一序列 | 编码器 |
| **作用** | 序列内部信息聚合 | 跨序列信息提取 |
| **典型场景** | 编码器、解码器第一子层 | 解码器第二子层 |

---

### 6.12 解码器的残差连接与层归一化

解码器的每个子层（掩码自注意力、交叉注意力、前馈网络）同样采用“残差连接 + 层归一化”的包装：

$$
\text{Output} = \text{LayerNorm}(x + \text{Sublayer}(x))
$$

三个子层的处理流程为：

$$
\begin{aligned}
x_1 &= \text{LayerNorm}(x + \text{MaskedAttention}(x)) \\
x_2 &= \text{LayerNorm}(x_1 + \text{CrossAttention}(x_1, \text{EncoderOutput})) \\
x_3 &= \text{LayerNorm}(x_2 + \text{FFN}(x_2))
\end{aligned}
$$

最终 $x_3$ 作为解码器层的输出，传递给下一层或最终的输出层。

---

### 6.13 最终输出层（Linear + Softmax）

解码器最后一层的输出 $x_3$ 经过一个线性层和 Softmax，得到下一个词的概率分布：

$$
P(\text{next word}) = \text{Softmax}(x_3 W_{\text{out}} + b_{\text{out}})
$$

其中 $W_{\text{out}} \in \mathbb{R}^{d_{\text{model}} \times V}$，$V$ 是词表大小。

- 线性层将 $d_{\text{model}}$ 维向量映射到词表维度 $V$。
- Softmax 将其转化为概率分布。
- 取概率最大的词（或按概率采样）作为输出。

---

### 6.14 Transformer 完整数据流总结

$$
\begin{aligned}
&\text{输入序列} \xrightarrow{\text{词嵌入 + 位置编码}} X \\
&\xrightarrow{\text{编码器层 × N}} \text{EncoderOutput} \\
&\xrightarrow{\text{解码器层 × N（掩码自注意力 + 交叉注意力 + FFN）}} \text{DecoderOutput} \\
&\xrightarrow{\text{线性层 + Softmax}} \text{下一个词的概率分布}
\end{aligned}
$$

> **一句话总结**：编码器通过自注意力理解输入，解码器通过掩码自注意力保持自回归特性、通过交叉注意力查询编码器输出，最终经过线性层和 Softmax 生成目标词。残差连接与层归一化贯穿始终，保障了深层网络的稳定训练。
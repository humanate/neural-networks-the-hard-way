### 7\.1 词嵌入与位置编码(input embedding & positional encoding )

Transformer的输入基础依然是词嵌入，将文本中的每个词语转换为固定维度的词向量。但由于Transformer完全抛弃了循环结构，没有了时序传递的过程，无法天然捕捉词语的位置信息，因此需要通过**位置编码（Positional Encoding）** 为每个词向量补充其在序列中的位置信息。

位置编码的核心逻辑是：为序列中每个位置生成一个与词向量维度相同的位置编码向量，将位置编码向量与对应位置的词向量相加，作为Transformer的最终输入。通过这种方式，输入向量中既包含了词语的语义信息，又包含了其在序列中的位置信息，解决了序列的语序建模问题。

论文《Attention Is All You Need》采用无需训练的固定公式：

对于序列中第 $ pos $ 个位置，编码向量的第 $ i $ 个维度定义为：

$$
PE(pos, 2i) = \sin\left( \frac{pos}{10000^{2i / d_{\text{model}}}} \right)
$$

$$
PE(pos, 2i+1) = \cos\left( \frac{pos}{10000^{2i / d_{\text{model}}}} \right)
$$

| 符号 | 含义 |
| :--- | :--- |
| $ pos $ | 词在序列中的位置索引（0, 1, 2, ...） |
| $ i $ | 维度索引（0 ≤ i < $ d_{model}/2 $） |
| $ d_{model} $ | 嵌入向量的维度（如 512） |

它能让模型不仅知道“绝对位置”，更能轻松学会“相对位置”。
优点：
1. “相对位置”的线性表示。位置 pos+k的编码向量，完全可以通过位置 pos 的编码向量，乘上一个“只跟 k 有关”的线性变换矩阵 T(k) 得到。
2. 无限外推能力（不用训练就能处理超长文本）。假设你的模型训练时最长只见过 512 个词的句子。如果用可学习位置编码，模型里只存了 512 个位置的参数。当推理时突然来了一句 1000 个词的文本，模型直接报错（因为查不到第 513 个位置的参数）。
3. 多频率融合。低维度（i=0）：数值像过山车一样剧烈抖动（高频），用来精准区分“紧挨着的两个词”（第 1 个和第 2 个）。高维度（i=2）：数值像乌龟爬一样缓慢变化（低频），用来区分“相隔很远的两个词”（第 1 个和第 100 个）。

假如我们直接用位置编号，比如 pos=1 时加 [1,1,1,1]，pos=2 时加 [2,2,2,2]：第 100 个词要加上巨大的数字 [100,100,100,100]，词义完全被淹没。而且 [1,1,1,1] 和 [2,2,2,2] 在几何上处在同一条直线上，模型很难区分“远近”和“大小”的区别。而正余弦编码把位置信息投射到了不同的频率空间（角度），让模型能用“旋转”的角度理解距离，而不是用“直线”的长度理解距离——这是它最精妙的地方。😊

现代模型（如 BERT、GPT）常采用可学习的参数矩阵：

| 对比维度 | 正弦/余弦固定编码 | 可学习位置编码 |
| :--- | :--- | :--- |
| **参数** | 无参数，直接计算 | 随机初始化，随训练更新 |
| **外推性** | ✅ 优秀（可处理超长序列） | ❌ 受限（推理长度不能超过训练长度） |
| **灵活性** | 固定模式 | 可适应特定数据分布 |

### 7\.2 核心机制：缩放点积注意力（Scaled Dot\-Product Attention）

注意力机制是Transformer的核心，其本质是：*为序列中的每个词，计算其与序列中所有词的语义相关性，再基于相关性权重对所有词的特征进行加权求和，生成该词包含全局上下文信息的新特征向量。*

缩放点积注意力的计算过程，分为三个核心步骤：

1. **QKV线性映射**：对于输入的词向量矩阵$X$，通过三个可学习的权重矩阵$W_Q$、$W_K$、$W_V$，分别线性映射生成三个矩阵：查询矩阵$Q$（Query）、键矩阵$K$（Key）、值矩阵$V$（Value），三个矩阵的维度与输入矩阵保持一致。

    $Q = XW_Q, \quad K = XW_K, \quad V = XW_V$

2. **相关性权重计算**：通过$Q$与$K$的点积，计算序列中每个词与所有词的语义相关性，再通过$\sqrt{d_k}$进行缩放（$d_k$为$K$的维度），避免点积值过大导致Softmax函数梯度消失，最后通过Softmax函数将相关性数值归一化为0\-1之间的权重系数，且所有权重之和为1。

3. **加权求和生成输出**：将归一化后的相关性权重矩阵与$V$矩阵相乘，对所有词的Value特征进行加权求和，最终得到注意力机制的输出矩阵，其中每个词的向量都包含了序列的全局上下文信息。

缩放点积注意力的完整数学表达式为：

$\text{Attention}(Q, K, V) = \text{softmax}\left(\frac{Q K^T}{\sqrt{d_k}}\right) V$

通过自注意力机制，序列中任意两个词之间的关联都可以直接计算，无需经过逐时刻的传递，彻底解决了长序列的长期依赖丢失问题；同时，整个序列的注意力计算可以完全并行化，无需等待时序计算，充分利用了GPU的并行计算能力，解决了RNN的效率瓶颈。


### 7\.3 能力升级：多头注意力机制（Multi\-Head Attention）

单头的缩放点积注意力仅能从一个维度学习词与词之间的关联，灵活性与特征捕捉能力有限。为了提升模型的特征表达能力，Transformer引入了**多头注意力机制**。

多头注意力的核心逻辑是：将$Q$、$K$、$V$矩阵通过多个独立的线性投影，拆分为$h$组（头），每组独立进行缩放点积注意力计算，得到$h$组独立的注意力输出；再将这$h$组输出进行拼接，通过一个线性变换矩阵$W^O$进行融合，最终得到多头注意力的输出。

其数学表达式为：

$\text{MultiHead}(Q, K, V) = \text{Concat}(\text{head}_1, \text{head}_2, ..., \text{head}_h) W^O$

$\text{where } \text{head}_i = \text{Attention}(Q W_i^Q, K W_i^K, V W_i^V)$

多头注意力机制允许模型在多个不同的表示子空间中学习语义关联，不同的注意力头可以捕捉到不同类型的语义关系，例如语法关系、指代关系、逻辑关联等，大幅提升了模型的语义理解与特征捕捉能力。


### 7.4 能力保障：残差连接与层归一化（Add & Norm）

随着网络层数的加深，Transformer 面临着深层网络共有的两大顽疾：**梯度消失/爆炸**与**内部协变量偏移**。仅依靠多头注意力和前馈网络堆叠，模型在训练时极易出现难以收敛、训练震荡甚至完全无法训练的情况。为了解决这一问题，Transformer 引入了**残差连接**与**层归一化**两项关键设计，它们共同构成了每个子层（Sub-layer）的“标配包装”。

其基本结构为：

$$
\text{Output} = \text{LayerNorm}(x + \text{Sublayer}(x))
$$

其中，`Sublayer(x)` 可以是多头注意力层或前馈网络层，`x` 为该子层的原始输入。以下分别解释这两项技术的作用与数学原理。

---

#### 7.4.1 残差连接（Residual Connection）

**核心逻辑**：将子层的输入直接“跳过”该子层，与子层的输出进行逐元素相加。

其数学表达式为：

$$
\text{Residual}(x) = x + \text{Sublayer}(x)
$$

**为什么需要它？**

在反向传播中，梯度通过链式法则逐层传递。对于深层网络，梯度需要连乘每一层的局部导数。如果这些导数普遍小于 1，梯度会指数级衰减（梯度消失）；如果普遍大于 1，则会指数级膨胀（梯度爆炸）。

**残差连接的加法操作，为梯度提供了一条“高速公路”**：

$$
\frac{\partial}{\partial x}(x + \text{Sublayer}(x)) = 1 + \frac{\partial \text{Sublayer}(x)}{\partial x}
$$

即使 `Sublayer(x)` 的导数非常小，梯度中始终存在一个恒等于 1 的“直通项”，保证梯度能够无衰减地回传到浅层。

**直观比喻**：残差连接就像在楼梯旁边修了一部电梯。即使楼梯（子层）因为太陡而难以攀爬（梯度消失），电梯（加法直通）也能把梯度直接送到目的地。

---

#### 7.4.2 层归一化（Layer Normalization）

**核心逻辑**：对每个样本的特征维度进行归一化，使其均值为 0、方差为 1，再通过可学习的缩放和平移参数恢复表达能力。

其数学表达式为：

$$
\text{LayerNorm}(x) = \gamma \cdot \frac{x - \mu}{\sqrt{\sigma^2 + \epsilon}} + \beta
$$

其中：

- `μ`：该样本在特征维度上的均值。
- `σ²`：该样本在特征维度上的方差。
- `ε`：极小常数（如 `10^{-6}`），防止分母为 0。
- `γ`：可学习的缩放参数（初始化为 1）。
- `β`：可学习的平移参数（初始化为 0）。

**与批归一化（BatchNorm）的关键区别**：

| 对比维度 | 层归一化（LayerNorm） | 批归一化（BatchNorm） |
| :--- | :--- | :--- |
| **归一化维度** | 对单个样本的**特征维度** | 对单个批次的**样本维度** |
| **依赖批次大小** | 不依赖，逐样本独立计算 | 强依赖，批次小则统计量不准 |
| **适用场景** | 序列长度可变（如 NLP） | 固定尺寸输入（如 CNN 图像） |
| **训练/推理一致性** | 完全一致 | 训练用批次统计量，推理用全局统计量 |

**为什么 Transformer 用 LayerNorm 而不用 BatchNorm？**

因为 NLP 任务中，不同句子的长度差异极大，且每个位置的特征分布随上下文动态变化。如果使用 BatchNorm，会对不同样本的同一位置做归一化，这既不合理（不同句子的同一位置可能毫无关联），也无法处理变长序列。LayerNorm 对每个样本独立归一化，天然适配序列建模。

---

#### 7.4.3 两者的协同作用

残差连接和层归一化通常联合使用，构成 Transformer 中每个子层的标准包装。具体有两种主流排列方式：

**Post-LN（原论文采用）**：

$$
\text{Output} = \text{LayerNorm}(x + \text{Sublayer}(x))
$$

即先做残差相加，再做层归一化。缺点是归一化在残差之后，训练初期梯度较大，需要谨慎的学习率预热。

**Pre-LN（现代模型如 GPT 采用）**：

$$
\text{Output} = x + \text{Sublayer}(\text{LayerNorm}(x))
$$

即先对输入做层归一化，再送入子层，最后残差相加。优点是训练更稳定，无需学习率预热，已成为当前主流。

**两者的分工**：

- **残差连接**：负责**梯度的畅通传递**，解决深层网络的梯度消失问题。
- **层归一化**：负责**数值的稳定分布**，解决每层输入分布漂移导致的训练震荡问题。

二者一“通”一“稳”，共同保障了 Transformer 能够稳定堆叠数十甚至上百层。

---

### 7.5 前馈全连接神经网络（Position-wise Feed-Forward Networks）

在 Transformer 的每个编码器层和解码器层中，多头注意力之后都紧跟一个**前馈全连接神经网络**（FFN）。它独立地作用于序列中的每一个位置，对注意力层聚合后的上下文表示进行进一步的**非线性变换**与**特征升维再降维**。

#### 7.5.1 数学定义

前馈网络由两个线性变换组成，中间夹着一个 ReLU 激活函数。对序列中第 $i$ 个位置的输入 $x_i$，其计算为：

$$
\text{FFN}(x_i) = \text{ReLU}(x_i W_1 + b_1) W_2 + b_2
$$

其中：

- $x_i \in \mathbb{R}^{d_{\text{model}}}$：第 $i$ 个位置的输入向量（来自上一子层的输出）。
- $W_1 \in \mathbb{R}^{d_{\text{model}} \times d_{ff}}$：第一层权重矩阵，将维度从 $d_{\text{model}}$ 扩展到 $d_{ff}$。
- $b_1 \in \mathbb{R}^{d_{ff}}$：第一层偏置。
- $W_2 \in \mathbb{R}^{d_{ff} \times d_{\text{model}}}$：第二层权重矩阵，将维度从 $d_{ff}$ 压缩回 $d_{\text{model}}$。
- $b_2 \in \mathbb{R}^{d_{\text{model}}}$：第二层偏置。

在原论文中，$d_{\text{model}} = 512$，$d_{ff} = 2048$，即先升维 4 倍，再降回原维度。

---

#### 7.5.2 逐位置独立处理（Position-wise）

这是 FFN 最容易被误解的地方：**它看起来是一个全连接层，但它并不在序列维度上做任何交互。**

对于长度为 $n$ 的序列，输入矩阵为 $X \in \mathbb{R}^{n \times d_{\text{model}}}$。FFN 的计算为：

$$
\text{FFN}(X) = \text{ReLU}(X W_1 + b_1) W_2 + b_2
$$

**关键理解**：

| 操作 | 是否跨位置交互？ | 说明 |
| :--- | :--- | :--- |
| 矩阵乘法 $X W_1$ | ❌ 不交互 | 每个位置独立乘以同一个 $W_1$ |
| 加偏置 $+ b_1$ | ❌ 不交互 | 每个位置独立加同一个 $b_1$ |
| ReLU 激活 | ❌ 不交互 | 逐元素操作 |
| 矩阵乘法 $\cdot W_2$ | ❌ 不交互 | 每个位置独立乘以同一个 $W_2$ |
| 加偏置 $+ b_2$ | ❌ 不交互 | 每个位置独立加同一个 $b_2$ |

**换句话说**：FFN 对序列中每个位置使用**完全相同**的两层网络，但每个位置的计算**完全独立**，互不影响。位置之间的信息交互已经由前面的**多头注意力**完成了。

---

#### 7.5.3 为什么需要 FFN？

多头注意力本质上是**加权求和**（线性操作），即使加上 Softmax，其核心仍然是“从已有信息中提取并混合”。如果整个 Transformer 只有注意力层，模型的表达能力仍然受限于**线性组合**。

FFN 的引入解决了这个问题：

| 作用 | 说明 |
| :--- | :--- |
| **引入非线性** | ReLU 打破了线性映射的限制，使模型能拟合复杂的语义关系。 |
| **升维再降维** | 先将特征映射到高维空间（$d_{ff}$），在高维空间中更容易找到线性可分的表示，再压缩回原维度。 |
| **逐位置特征提取** | 对每个位置的上下文表示进行独立的深度加工，相当于“在注意力聚合信息之后，对每个词做一次深度思考”。 |
| **参数容量** | FFN 参数量通常占 Transformer 总参数的 **2/3**，是模型存储知识的主要载体。 |

---

#### 7.5.4 与多头注意力的分工

| 组件 | 核心职责 | 信息交互范围 |
| :--- | :--- | :--- |
| **多头注意力** | 从序列中**聚合上下文信息** | 跨位置（全局） |
| **前馈网络（FFN）** | 对聚合后的信息进行**非线性加工** | 逐位置（局部） |

两者交替堆叠，形成了 Transformer 层“**先看全局，再想局部**”的处理节奏。

---

#### 7.5.5 总结

> 前馈全连接网络是 Transformer 中每个子层后的“深度加工厂”。它通过“升维 → ReLU → 降维”的两层结构，对注意力层输出的每个位置独立地进行非线性变换，极大地增强了模型的表达能力。它与多头注意力交替配合，一个负责跨位置聚合信息，一个负责逐位置深度加工，共同构成了 Transformer 强大的表征能力。


### 7.6 前馈网络后的残差连接与层归一化

前馈网络（FFN）与多头注意力一样，也被包裹在“残差连接 + 层归一化”的标准子层中。其计算流程为：

$$
\text{Output} = \text{LayerNorm}(x + \text{FFN}(x))
$$

其中 $x$ 是 FFN 子层的输入（即上一节中多头注意力 + 残差 + 归一化后的输出），$\text{FFN}(x)$ 是前馈网络的输出。

---

#### 3. 层归一化

对 $R_{\text{FFN}}$ 的每一行独立进行层归一化。设 $\gamma = 1$、$\beta = 0$、$\epsilon = 10^{-6}$。

---

#### 5. 编码器层的完整数据流回顾

至此，我们完成了 Transformer **编码器层**的完整前向计算。整个流程总结如下：

| 步骤 | 操作 | 输入 | 输出 |
| :--- | :--- | :--- | :--- |
| **1** | 词嵌入 + 位置编码 | 文字“程序”、“员” | $X$（2×4 矩阵） |
| **2** | 多头注意力 | $X$ | $Z_{\text{MultiHead}}$（2×4） |
| **3** | 残差 + 层归一化 | $X$ 与 $Z_{\text{MultiHead}}$ | $X_{\text{in}}$（2×4） |
| **4** | 前馈网络（FFN） | $X_{\text{in}}$ | $\text{FFN}(X_{\text{in}})$（2×4） |
| **5** | 残差 + 层归一化 | $X_{\text{in}}$ 与 $\text{FFN}(X_{\text{in}})$ | **EncoderOutput**（2×4） |

> **注意**：实际 Transformer 中，编码器层会堆叠 $N$ 次（原论文 $N=6$）。每一层的输出作为下一层的输入，逐层提取更深层的语义特征。

---
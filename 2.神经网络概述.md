## 2 神经网络参数求解：优化目标与核心算法

神经网络的核心目标，是找到一组最优的权重$W$和偏置$b$，使得模型的预测结果与真实值的偏差最小化。这一过程，即为神经网络的参数求解与模型训练。

### 2\.1 优化目标的量化：损失函数

要实现参数的优化，首先需要对“预测结果与真实值的偏差”进行数学量化，完成这一量化的函数，即为**损失函数**。损失函数是关于模型所有可学习参数（$W,b$）的多元函数，其值越小，代表模型的拟合效果越好。

![image\.png](图片/image%2010.png)

回归任务中最基础、最常用的损失函数为**均方误差（Mean Squared Error, MSE）**，其表达式为：

$L(W, b) = \frac{1}{N} \sum_{i=1}^{N} \left(y_{i} - \hat{y}_{i}\right)^2$

其中：

- $N$为样本总数；

- $y_i$为第$i$个样本的真实值；

- $\hat{y}_i$为模型对第$i$个样本的预测值。

神经网络的训练目标，转化为了一个数学优化问题：**求解使得损失函数**$L(W,b)$**取得最小值的参数**$W$**和**$b$**。**



### 2\.2 解析解的困境：深层非线性网络的优化挑战

对于简单的线性回归模型，我们可以通过最小二乘法，令损失函数对每个参数的偏导数等于0，直接求解得到参数的解析解。

但对于深层神经网络而言，其本质是一个高度复杂的非线性复合函数，对应的损失函数具有**非凸、高维、非线性**的特征，不存在通用的解析解。因此，深度学习中普遍采用**迭代式的数值优化方法**，其中最核心、最基础的算法为**梯度下降法**。



### 2\.3 梯度下降法：迭代式参数优化的核心框架

梯度下降法的核心逻辑，基于梯度的数学定义：**损失函数对所有可学习参数的偏导数构成的向量，即为梯度**。梯度的方向，是损失函数在当前参数点上升最快的方向；反之，梯度的反方向，就是损失函数下降最快的方向。

因此，我们只需让参数沿着梯度的反方向进行迭代更新，即可让损失函数持续下降，最终收敛到最小值附近。其参数更新的通用公式为：

$W = W - \eta \cdot \frac{\partial L(W,b)}{\partial W}$

$b = b - \eta \cdot \frac{\partial L(W,b)}{\partial b}$

其中$\eta$为**学习率**，是控制参数更新步长的超参数：学习率过大会导致参数震荡，无法收敛；学习率过小会导致收敛速度过慢，甚至陷入局部最优。

通过不断重复“计算梯度\-更新参数”的过程，损失函数会持续降低，模型的拟合效果也会持续提升。



### 2\.4 反向传播：基于链式法则的高效梯度计算

梯度下降法的核心前提，是计算损失函数对每一层每一个参数的偏导数。对于深层神经网络，多层复合函数的偏导数计算，需要通过**链式法则**完成拆解，而**反向传播算法**，正是链式法则在深层神经网络中的工程化实现。

我们以一个极简的两层神经网络为例，说明反向传播的核心逻辑：

网络的前向传播流程为：

$x \stackrel{g\left(w_{1} x+b_{1}\right)}{\to} a \stackrel{g\left(w_{2} a+b_{2}\right)}{\to} \hat{y} \stackrel{(y-\hat{y})^{2}}{\to} L$

若要求解损失函数对第一层权重$w_1$的偏导数，通过链式法则可拆解为：

$\frac{\partial L}{\partial w_{1}} = \frac{\partial L}{\partial \hat{y}} \cdot \frac{\partial \hat{y}}{\partial a} \cdot \frac{\partial a}{\partial w_{1}}$

反向传播的核心，就是**从输出层到输入层，从右向左逐层计算偏导数，并复用中间的梯度计算结果**。这种方式避免了重复计算，大幅提升了深层网络的梯度计算效率，是深度学习能够实现深层网络训练的核心基础。

![image\.png](图片/image%207.png)



### 2\.5 神经网络的完整训练流程

结合前向传播、损失计算、反向传播与梯度下降，神经网络的一次完整训练迭代（step）流程如下：

1. **前向传播**：将训练数据输入网络，完成从输入层到输出层的全流程计算，得到模型的预测结果；

2. **损失计算**：基于预测结果与真实标签，通过损失函数计算当前模型的损失值；

3. **反向传播**：基于链式法则，从输出层到输入层逐层计算损失函数对每个可学习参数的梯度；

4. **参数更新**：通过梯度下降法，沿着梯度的反方向更新网络的所有权重与偏置。

重复上述迭代过程，直到损失函数收敛到预设阈值，即可得到一组拟合效果最优的模型参数，完成神经网络的训练。

---
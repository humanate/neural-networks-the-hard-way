## 5 循环神经网络（RNN）：面向时序数据的序列建模方案

自然语言处理、语音识别、时间序列预测等任务的核心，是对序列数据的建模，这类数据的核心特征是：数据的语义与规律，不仅与当前时刻的输入相关，还与之前时刻的序列信息强相关，且序列长度不固定。RNN正是为解决序列数据的建模问题而设计的专用神经网络架构。

### 5\.1 文本数据的向量化：从离散符号到连续语义空间

神经网络只能处理数值型数据，因此在对文本进行建模前，首先需要将离散的文字、词语符号转换为连续的数值向量，这一过程被称为文本的向量化。文本向量化技术经历了三代核心演进：

1. **单值离散映射**：最基础的向量化方式，为每一个字/词分配一个唯一的数字ID，建立“词语\-数字”的映射表。这种方式存在两个致命缺陷：一是仅用一维数字表示词语，维度极低，无法承载丰富的语义信息；二是数字之间的数值关系无法表示词语之间的语义关联，无法建模词语之间的相似性与逻辑关系。

2. **独热编码（One\-hot Encoding）**：为解决维度缺陷提出的方案，为每个词语分配一个与词表长度相同的高维向量，向量中仅该词语对应的位置为1，其余所有位置均为0。这种方式解决了维度问题，但带来了新的缺陷：向量维度极高、极度稀疏，且依然无法表示词语之间的语义关联，两个语义相近的词语，其独热编码向量之间的余弦相似度为0，完全无法体现语义相关性。

3. **词嵌入（Word Embedding）**：当前自然语言处理领域的标准文本向量化方案，其核心是通过模型训练，为每个词语学习一个低维、稠密的连续向量，即词向量。词向量的每个维度对应一个可解释或不可解释的语义特征，词语之间的语义相关性，可通过词向量之间的点积或余弦相似度进行量化计算。

训练完成的词嵌入矩阵，能够精准捕捉词语之间的语义与逻辑关系，例如经典的语义关系示例：$\text{桌子}-\text{椅子} \approx \text{鼠标}-\text{键盘}$。所有词语的词向量共同组成的矩阵，即为嵌入矩阵，其中的数值均为模型的可学习参数，Word2Vec是最经典的词嵌入预训练方法。

词向量所在的高维语义空间，被称为**潜空间（Latent Space）**，可通过降维算法（如PCA、t\-SNE）将其降至2\-3维，实现词与词语义关系的可视化。

### 5\.2 RNN的核心设计：时序信息的传递与建模

词嵌入解决了文本的向量化问题，但直接将整句话的所有词向量拼接后输入全连接网络，依然存在两个核心问题：一是序列长度不固定，导致输入层维度无法固定；二是完全丢失了词语的先后顺序信息，而语序正是自然语言语义的核心载体。

RNN的核心设计，完美解决了这两个问题：**RNN不再一次性输入整句话的所有内容，而是按序列顺序逐个输入每个时刻的词向量，同时通过隐藏状态（Hidden State）传递之前时刻的序列信息**，实现了对时序依赖关系的建模。

对于$t$时刻的输入，RNN的核心计算公式为：

$h_t = g\left(W_{xh}x_t + W_{hh}h_{t-1} + b_h\right)$

$y_t = g_2\left(W_{hy}h_t + b_y\right)$

其中：

- $x_t$为$t$时刻的输入词向量；

- $h_{t-1}$为$t-1$时刻的隐藏状态，承载了之前所有时刻的序列信息；

- $W_{xh}$、$W_{hh}$、$W_{hy}$为可学习的权重矩阵，$b_h$、$b_y$为偏置向量；

- $h_t$为$t$时刻输出的隐藏状态，将传递给$t+1$时刻；

- $y_t$为$t$时刻的模型输出。

通过这种循环结构，RNN具备了对序列数据的时序建模能力，能够捕捉词语的先后顺序关系，可完成文本分类、语言生成、机器翻译等各类自然语言处理任务。

![image\.png](图片/image%205.png)

![image\.png](图片/image%206.png)

### 5\.3 RNN的固有缺陷：长期依赖丢失与串行计算瓶颈

RNN解决了序列数据的基础建模问题，但存在两个无法彻底解决的核心缺陷，这也是后续Transformer架构诞生的核心诱因：

1. **长期依赖丢失问题**：RNN的时序信息仅通过隐藏状态逐时刻传递，随着序列长度（时间步）的增加，早期时刻的信息会在传递过程中不断衰减，最终完全丢失，模型无法捕捉长序列中远距离的语义依赖关系。而自然语言中，很多关键的语义信息恰恰存在于长距离的上下文之中。尽管LSTM、GRU等改进型RNN通过门控机制一定程度上缓解了这一问题，但依然无法彻底解决长序列的信息丢失问题。

2. **串行计算的效率瓶颈**：RNN的计算过程必须严格遵循序列的先后顺序，$t$时刻的计算必须等待$t-1$时刻的隐藏状态计算完成后才能开始，无法实现并行计算。随着序列长度的增加，计算耗时会线性增长，在大模型、长序列的场景下，计算效率极低，无法充分利用GPU的并行计算能力。

RNN的这两大核心缺陷，催生了自然语言处理领域的范式革命——Transformer架构的诞生。


### 5\.4 LSTM（Long Short-Term Memory，长短期记忆网络）
LSTM是一种特殊的循环神经网络，它被设计出来的唯一目的，就是为了解决你在前面问到的那个核心问题——“链式法则连乘导致的梯度消失/爆炸”。简单说：RNN 记不住太久远的事，而 LSTM 专门给自己加了一个“备忘录”来解决这个问题。

LSTM 的核心创新：细胞状态（传送带）
LSTM 引入了一个全新的概念，叫 细胞状态（Cell State）。你可以把它想象成一条贯穿整个网络的“传送带”。

这条传送带上的信息极少被干扰，可以一路畅通无阻地从序列开头传到结尾。

![alt text](image-1.png)为

遗忘门：决定从上一时刻的旧记忆细胞中丢弃哪些不再重要的信息。
输入门：决定当前时刻的新知识有多大的价值，值不值得存入长期记忆档案柜（细胞状态）。
输出门：决定此时此刻，应该把细胞状态里的哪些内容解密并输出给外界（作为当前时刻的隐藏状态。
候选记忆细胞是 LSTM 的“内容生成器”，它把当前的输入转换成一份标准格式（-1~1）的“新知识草稿”；至于这份草稿能不能真正存入档案柜，还要看输入门（的签字盖章。

LSTM存在的问题：
1. 无法并行计算（串行依赖）。当你训练一个包含 2048 个词的句子时，GPU 虽有成千上万个核心，但无法同时利用——只能排队等待。这使得 LSTM 无法利用现代 GPU 的并行算力，训练极慢。
2. “长时记忆”依然有限（理论美好，现实骨感）。LSTM 通常只能有效处理 100~300 个词的长度。当你需要处理万字长文档时，LSTM 必然会把开头讲的内容忘得一干二净。
3. 信息压缩瓶颈（必须把所有过去塞进一个向量）。在处理超长序列时，LSTM 即使记住了开头，中间的大段信息也会因为“抽屉满了”而被挤压变形。
4. 参数臃肿与训练不稳定（极难调参）。LSTM 内部有 4 个全连接层（遗忘门、输入门、输出门、候选记忆），而普通 RNN 只有 1 个。

虽然有一些方法在改进以上两点问题，但是还不够好，那么是否有一个可以彻底抛弃按顺序计算的新方案呢？

有的兄弟，有的，那就是Transformer！

---
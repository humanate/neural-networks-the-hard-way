## 6 Transformer：基于自注意力机制的序列建模范式革命

Transformer架构于2017年在《Attention Is All You Need》论文中被首次提出，其彻底抛弃了RNN的循环结构，完全基于自注意力机制实现序列建模，同时解决了RNN的长期依赖丢失与串行计算两大核心缺陷，成为当前大语言模型的核心基础架构。
### 6\.1 Transformer的整体架构：编码器\-解码器体系

Transformer采用经典的**编码器\-解码器（Encoder\-Decoder）** 架构，其中编码器负责对输入序列进行编码，提取全局上下文特征；解码器负责基于编码器的输出，自回归地生成目标序列。

![image\.png](图片/image%201.png)

#### 6\.1\.1 编码器（Encoder）

编码器由$N$个完全相同的编码器层堆叠而成，每个编码器层包含两个核心子层：

1. **多头自注意力层**：对输入序列进行自注意力计算，为每个词生成包含全局上下文信息的特征向量；

2. **前馈神经网络层（Feed Forward Network, FFN）**：一个两层的全连接神经网络，对每个位置的特征向量进行独立的非线性变换，进一步提升特征表达能力。

每个子层都采用了**残差连接（Residual Connection）\+ 层归一化（Layer Normalization）** 的结构，即子层的输入与输出相加后进行归一化，有效缓解了深层网络的梯度消失问题，让数十层甚至上百层的Transformer网络能够稳定训练。

#### 6\.1\.2 解码器（Decoder）

解码器由$N$个完全相同的解码器层堆叠而成，每个解码器层包含三个核心子层：

1. **掩码多头自注意力层（Masked Multi\-Head Attention）**：在自注意力计算中加入掩码机制，在训练时遮住当前位置之后的所有词的信息，避免模型在生成当前词时“看到”未来的序列内容，模仿真实推理时的自回归生成过程；

2. **交叉注意力层（Encoder\-Decoder Attention）**：将编码器的输出作为$K$和$V$，将掩码注意力层的输出作为$Q$，计算注意力权重，让解码器的生成过程能够关注到输入序列的全局上下文信息，适配机器翻译、文本摘要等序列到序列的任务；

3. **前馈神经网络层**：与编码器中的前馈层结构一致，对特征进行进一步的非线性变换。

与编码器相同，解码器的每个子层也都采用了残差连接\+层归一化的结构，保证深层网络的稳定训练。

解码器的最终输出会通过一个线性层与Softmax函数，转换为词表中每个词语的生成概率，最终完成序列的生成任务。